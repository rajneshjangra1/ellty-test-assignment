# Ellty Test Assignment - React Starter (Vite)
This is a minimal React starter that uses Vite. To run:
1. npm install
2. npm run dev
You can replace App.js/App.css with your Figma-based component implementation.
